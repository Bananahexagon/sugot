# 変数と定数

```
// 整数値の変数aを宣言し、5を代入する
let a: i32 = 5

// 先に初期化だけを行い、後から値を代入することもできます。
let b: i32
b = 5

// 初期化前の変数を使うとコンパイルエラーが出ます。
let c: i32
print(c) // コンパイルエラー

// 宣言と同時に初期化する場合は、型推論を利用できる可能性があります。
let d = 5 // i32型に自動で推論される

// constを使うと定数を宣言することができます。
const e = 5 // 必ず同時に初期化を行う
```
